package com.example.stageonda.Repo;

import com.example.stageonda.Entities.Reception;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Reception_Repo extends JpaRepository<Reception,Long> {
}
